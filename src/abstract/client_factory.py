"""Factory pattern for virtual machine client creation.

I suppose we should use this pattern because it helps us
to create new type of virtual machine clients dynamically.
Virtual machine with different attributes, for example,
different OS can be have different logic.
"""
